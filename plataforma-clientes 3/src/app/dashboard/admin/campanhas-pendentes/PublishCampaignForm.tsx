// Arquivo não usado mais — a tela "Campanhas ativas" foi reformulada pra
// uma lista única agrupada por ministério com toggle de visibilidade (ver
// CampaignsAdminTable.tsx / CampaignRow.tsx). Mantido só porque o ambiente
// não conseguiu apagar o arquivo; pode excluir com segurança quando puder.
export {};
